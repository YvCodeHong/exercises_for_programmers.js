//Create a program that compares two strings and determines if the two strings 
// are anagrams. The program should prompt for both input strings and display the 
//the output as shown in the example that follows. 

function anagramCheckers(firstString, secondString) {
    const input = prompt("Enter two strings and I'll tell you if they are anagrams");
    alert(firstString + secondString + "are angrams");
};


