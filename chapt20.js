//Create a tax calculator that handles multiple states and multiple counties
// within each state. The program prompts the user for the order amount 
// and the state where the order will be shipped 

const taxCalculator = () => {
    const amount = prompt("What is the order amount?");
    const state = prompt("What state do you live in");
    const tax = 0.50
}